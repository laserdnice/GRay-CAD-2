# Laufzeit-Abhängigkeiten von GRay-CAD 2.
# Nur Pakete, die im Code tatsächlich importiert werden.
# Untere Schranken (>=) sichern Python-3.12-Kompatibilität; solange nichts
# bricht, bewusst nicht nach oben gepinnt, damit die Installation nicht an
# veralteten Versionen scheitert.
numpy>=1.26
scipy>=1.11
PyQt5>=5.15.9
pyqtgraph>=0.13.3
numba>=0.59
deap>=1.4
Pint>=0.23
